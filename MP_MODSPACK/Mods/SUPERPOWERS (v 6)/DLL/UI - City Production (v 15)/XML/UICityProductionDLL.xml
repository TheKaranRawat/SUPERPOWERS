<?xml version="1.0" encoding="utf-8"?>
<GameData>
	<!-- MUST be in a file on its own, so it can fail if the DLL is not in use -->
	<CustomModOptions>
		<Update>
			<Where Name="UI_CITY_PRODUCTION"/>
			<Set Value="1"/>
		</Update>
	</CustomModOptions>
</GameData>